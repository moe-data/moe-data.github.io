
  function cate(e){
    z(sc=e)
    loadlist()
  }

  loadlist()
  
  const width = Width()-80// container.scrollWidth*1.34;
  const height = (width || 500) - 20;
  // $('#container').css('width',width*2)
    $("#nav").load("nav.html");
    // $("#foot").load("../foot.html");