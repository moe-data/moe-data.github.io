name: dump

on:
  push:
    paths:
      - '*dump.yml'
  schedule:
    - cron: '0 2 * * *'  # 每天 UTC 2点（北京时间10点）执行
  workflow_dispatch:     # 手动触发  push:

jobs:
  check-and-download:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.check.outputs.changed }}
      month: ${{ steps.set-month.outputs.month }}
      d_changed: ${{ steps.pre-check.outputs.d_changed }}
      c_changed: ${{ steps.pre-check.outputs.c_changed }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set current month
        id: set-month
        run: echo "month=$(date +%Y%m)" >> $GITHUB_OUTPUT

      # 核心改1：安装Git LFS（用于推送gz文件）
      - name: Install Git LFS
        run: |
          sudo apt-get update && sudo apt-get install -y git-lfs
          git lfs install
          # 跟踪 drop*.gz 文件
          git lfs track "dump/drop*.gz"


      # 核心改2：先仅获取ETag（不下载文件），减少网络开销
      - name: Get ETag only (no file download)
        id: get-etag
        # 切换为PowerShell shell执行ETag获取
        shell: pwsh
        run: |
          $MONTH="${{ steps.set-month.outputs.month }}"
          echo "======= 【前置】仅获取ETag + 自检 ======="
          
          # 初始化ETag变量+自检标记
          echo "--- 处理 createitemrecords ---"
          $developEtag = $null
          # 极简PowerShell获取ETag，去除引号/空格
          $developEtag = (Invoke-WebRequest -Uri "https://api.poi.moe/dump/createitemrecords.gz" -Method Head).Headers['ETag'] -replace '["\s]', ''
          echo "develop ETag 读取状态: success"
          echo "develop 最新 ETag: $developEtag"
          # 自检：ETag为空直接exit 2
          if ([string]::IsNullOrEmpty($developEtag)) {
            echo "❌ develop ETag 为空，终止流程"
            exit 2
          }
          # 输出到GitHub变量
          echo "DEVELOP_ETAG=$developEtag" >> $env:GITHUB_OUTPUT
          echo "DEVELOP_ETAG_STATUS=success" >> $env:GITHUB_OUTPUT
          
          # ===== 处理 createshiprecords ETag =====
          echo "--- 处理 createshiprecords ---"
          $constructEtag = (Invoke-WebRequest -Uri "https://api.poi.moe/dump/createshiprecords.gz" -Method Head).Headers['ETag'] -replace '["\s]', ''
          echo "construct ETag 读取状态: success"
          echo "construct 最新 ETag: $constructEtag"
          # 自检：ETag为空直接exit 2
          if ([string]::IsNullOrEmpty($constructEtag)) {
            echo "❌ construct ETag 为空，终止流程"
            exit 2
          }
          # 输出到GitHub变量
          echo "CONSTRU_ETAG=$constructEtag" >> $env:GITHUB_OUTPUT
          echo "CONSTRU_ETAG_STATUS=success" >> $env:GITHUB_OUTPUT

      # 核心改3：先读取历史ETag，对比后决定是否下载/取消工作流
      - name: Check ETag change before download
        id: pre-check
        shell: /usr/bin/bash --noprofile --norc -e -o pipefail {0}
        run: |
          MONTH="${{ steps.set-month.outputs.month }}"
          MONTHLY_SIZE_FILE="dump/monthly_size.txt"
          mkdir -p dump # 确保dump目录存在

          # 初始化变更标记
          d_changed="true"
          c_changed="true"
          # 初始化历史ETag变量
          HIST_DEVELOP_ETAG=""
          HIST_CONSTRU_ETAG=""

          echo "======= 读取历史 monthly_size.txt（c/d 分开）======="
          # 读取monthly_size.txt中的历史ETag（键值格式）
          if [ -f "$MONTHLY_SIZE_FILE" ]; then
            # 修复核心：优化提取命令，步骤如下
            # 1. sed 's/^[ \t]*//; s/[ \t]*$//' 去除行首行尾空白/制表符
            # 2. grep -E 精准匹配，增加 -a 处理隐藏二进制字符
            # 3. awk 拆分后先清理空白，再提取值
            # 4. tr -d '"' 去除引号，同时处理空值场景
            HIST_DEVELOP_ETAG=$(sed 's/^[ \t]*//; s/[ \t]*$//' "$MONTHLY_SIZE_FILE" | grep -a -E "^d${MONTH}_ETAG=" | awk -F'=' '{gsub(/^[ \t"]+|[ \t"]+$/,"",$2); print $2}')
            HIST_CONSTRU_ETAG=$(sed 's/^[ \t]*//; s/[ \t]*$//' "$MONTHLY_SIZE_FILE" | grep -a -E "^c${MONTH}_ETAG=" | awk -F'=' '{gsub(/^[ \t"]+|[ \t"]+$/,"",$2); print $2}')
            echo "历史 d${MONTH} ETag: $HIST_DEVELOP_ETAG"
            echo "历史 c${MONTH} ETag: $HIST_CONSTRU_ETAG"
          else
            echo "======= monthly_size.txt不存在，首次执行 ======="
            echo "d_changed=$d_changed" >> $GITHUB_OUTPUT
            echo "c_changed=$c_changed" >> $GITHUB_OUTPUT
            exit 0
          fi

          # ===== 分开判断 d 的 ETag（仅读取成功时才对比）=====
          echo "--- 对比 develop ETag ---"
          if [ "${{ steps.get-etag.outputs.DEVELOP_ETAG_STATUS }}" = "success" ] && [ -n "$HIST_DEVELOP_ETAG" ] && [ -n "${{ steps.get-etag.outputs.DEVELOP_ETAG }}" ]; then
            if [ "${{ steps.get-etag.outputs.DEVELOP_ETAG }}" = "$HIST_DEVELOP_ETAG" ]; then
              d_changed="false"
              echo "develop ETag 完全一致，无更新"
            else
              d_changed="true"
              echo "develop ETag 不一致（历史：$HIST_DEVELOP_ETAG | 最新：${{ steps.get-etag.outputs.DEVELOP_ETAG }}）"
            fi
          else
            d_changed="true"
            echo "develop ETag 读取失败/历史不存在，判定为有更新"
          fi

          # ===== 分开判断 c 的 ETag（仅读取成功时才对比）=====
          echo "--- 对比 construct ETag ---"
          if [ "${{ steps.get-etag.outputs.CONSTRU_ETAG_STATUS }}" = "success" ] && [ -n "$HIST_CONSTRU_ETAG" ] && [ -n "${{ steps.get-etag.outputs.CONSTRU_ETAG }}" ]; then
            if [ "${{ steps.get-etag.outputs.CONSTRU_ETAG }}" = "$HIST_CONSTRU_ETAG" ]; then
              c_changed="false"
              echo "construct ETag 完全一致，无更新"
            else
              c_changed="true"
              echo "construct ETag 不一致（历史：$HIST_CONSTRU_ETAG | 最新：${{ steps.get-etag.outputs.CONSTRU_ETAG }}）"
            fi
          else
            c_changed="true"
            echo "construct ETag 读取失败/历史不存在，判定为有更新"
          fi

          # 输出变更标记到GitHub变量
          echo "d_changed=$d_changed" >> $GITHUB_OUTPUT
          echo "c_changed=$c_changed" >> $GITHUB_OUTPUT
          # 输出整体变更标记（用于后续步骤）
          if [ "$d_changed" = "false" ] && [ "$c_changed" = "false" ]; then
            echo "整体无变更（c/d ETag 均一致）"
          else
            echo "整体有变更（c/d 至少一个有变化）"
          fi

      # 修复3：第一次取消工作流（仅 c/d ETag 均无变化时执行）
      - name: 取消工作流（ETag无变化）
        if: steps.pre-check.outputs.d_changed == 'false' && steps.pre-check.outputs.c_changed == 'false'
        uses: conbanwa/cancel-workflow/.github/actions/cancel-workflow@v1.0.0
        with:
          cancel-reason: 'ETag无变化，无需下载文件，自动取消工作流'

      # 核心改5：仅ETag变化时才下载文件 + 自检文件存在性
      - name: Download c*.gz (only if c ETag changed)
        if: steps.pre-check.outputs.d_changed == 'true'
        run: |
          echo "======= 开始下载 createitemrecords.gz ======="
          curl -s -o develop.gz https://api.poi.moe/dump/createitemrecords.gz
          # 自检：文件不存在直接exit 2
          if [ ! -f "develop.gz" ]; then
            echo "❌ develop.gz不存在，终止流程"
            exit 2
          fi
          # 计算MD5 + 自检空值
          MD5_DEVELOP_GZ=$(md5sum develop.gz | awk '{print $1}')
          if [ -z "$MD5_DEVELOP_GZ" ] || [ "$MD5_DEVELOP_GZ" = "N/A" ]; then
            echo "❌ develop.gz MD5 为空，终止流程"
            exit 2
          fi
          echo "✅ develop.gz 最新MD5: '$MD5_DEVELOP_GZ'"

      - name: Download d*.gz (only if d ETag changed)
        if: steps.pre-check.outputs.c_changed == 'true'
        run: |
          echo "======= 开始下载 createshiprecords.gz ======="
          curl -s -o construct.gz https://api.poi.moe/dump/createshiprecords.gz
          # 自检：文件不存在直接exit 2
          if [ ! -f "construct.gz" ]; then
            echo "❌ construct.gz不存在，终止流程"
            exit 2
          fi
          # 计算MD5 + 自检空值
          MD5_CONSTRU_GZ=$(md5sum construct.gz | awk '{print $1}')
          if [ -z "$MD5_CONSTRU_GZ" ] || [ "$MD5_CONSTRU_GZ" = "N/A" ]; then
            echo "❌ construct.gz MD5 为空，终止流程"
            exit 2
          fi
          echo "✅ construct.gz 最新MD5: '$MD5_CONSTRU_GZ'"

      - name: Check if changed
        id: check
        shell: /usr/bin/bash --noprofile --norc -e -o pipefail {0}
        run: |
          # 复用 pre-check 的结果，初始化整体变更标记
          d_changed="${{ steps.pre-check.outputs.d_changed }}"
          c_changed="${{ steps.pre-check.outputs.c_changed }}"
          echo "changed=$([ "$d_changed" = "true" ] || [ "$c_changed" = "true" ] && echo "true" || echo "false")" >> $GITHUB_OUTPUT
          
          if [ "$d_changed" = "false" ] && [ "$c_changed" = "false" ]; then
            exit 0 # 无变化时直接退出，不执行后续逻辑
          fi

          # 定义月份变量（避免嵌套引用问题）
          MONTH="${{ steps.set-month.outputs.month }}"
          D_HISTORY_GZ_FILE="dump/c${MONTH}.gz"
          C_HISTORY_GZ_FILE="dump/d${MONTH}.gz"
          MONTHLY_SIZE_FILE="dump/monthly_size.txt"
          
          echo "======= 打印ETag内容 ======="
          # 拼接最新ETag（保留原有变量名，最小化改动）
          DEVELOP_ETAG="ETag: ${{ steps.get-etag.outputs.DEVELOP_ETAG }}"
          CONSTRU_ETAG="ETag: ${{ steps.get-etag.outputs.CONSTRU_ETAG }}"
          echo "develop ETag (清理后): $DEVELOP_ETAG"
          echo "construct ETag (清理后): $CONSTRU_ETAG"
          
          echo "======= 打印文件MD5值 ======="
          # ===== 分开处理 d*.gz（MD5 检查，第二次取消的基础）=====
          MD5_DEVELOP_GZ="N/A"
          DEVELOP_SIZE="N/A"
          if [ "$d_changed" = "true" ] && [ -f "develop.gz" ]; then
            MD5_DEVELOP_GZ=$(md5sum develop.gz | awk '{print $1}')
            # 二次自检：MD5为空exit 2
            if [ -z "$MD5_DEVELOP_GZ" ]; then
              echo "❌ develop.gz MD5 为空，终止流程"
              exit 2
            fi
            echo "✅ develop.gz (最新文件) MD5: $MD5_DEVELOP_GZ"
            # 移动文件 + 计算大小
            mv develop.gz "$D_HISTORY_GZ_FILE"
            # 计算 develop 文件大小
            DEVELOP_SIZE=$(du -k "$D_HISTORY_GZ_FILE" | awk '{print $1}')
            echo "✅ $D_HISTORY_GZ_FILE 大小: ${DEVELOP_SIZE} KB"
          elif [ "$d_changed" = "false" ]; then
            echo "✅ develop.gz 无变化，跳过 MD5 计算"
          else
            echo "❌ develop.gz 文件不存在"
            exit 2
          fi

          # ===== 分开处理 c*.gz（MD5 检查，第二次取消的基础）=====
          MD5_CONSTRU_GZ="N/A"
          CONSTRU_SIZE="N/A"
          if [ "$c_changed" = "true" ] && [ -f "construct.gz" ]; then
            MD5_CONSTRU_GZ=$(md5sum construct.gz | awk '{print $1}')
            # 二次自检：MD5为空exit 2
            if [ -z "$MD5_CONSTRU_GZ" ]; then
              echo "❌ construct.gz MD5 为空，终止流程"
              exit 2
            fi
            echo "✅ construct.gz (最新文件) MD5: $MD5_CONSTRU_GZ"
            # 移动 construct.gz 到目标路径
            mv construct.gz "$C_HISTORY_GZ_FILE"
            # 计算 construct 文件大小
            CONSTRU_SIZE=$(du -k "$C_HISTORY_GZ_FILE" | awk '{print $1}')
            echo "✅ $C_HISTORY_GZ_FILE 大小: ${CONSTRU_SIZE} KB"
          elif [ "$c_changed" = "false" ]; then
            echo "✅ construct.gz 无变化，跳过 MD5 计算"
          else
            echo "❌ construct.gz 文件不存在"
            exit 2
          fi

          # ===== 保留：写入 monthly_size.txt（键值格式）======
          echo "======= 写入 monthly_size.txt（键值格式）======="
          # 获取当前检查时间（ISO 格式，方便解析）
          CHECK_TIME=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          
          # 先删除当前月份的旧记录（避免重复）
          grep -v -E "^[cd]${MONTH}_" "$MONTHLY_SIZE_FILE" > "$MONTHLY_SIZE_FILE.tmp" 2>/dev/null || true
          # 写入develop的键值
          if [ "$d_changed" = "true" ]; then
            echo "d${MONTH}_ETAG=\"${{ steps.get-etag.outputs.DEVELOP_ETAG }}\"" >> "$MONTHLY_SIZE_FILE.tmp"
            echo "d${MONTH}_MD5=\"${MD5_DEVELOP_GZ}\"" >> "$MONTHLY_SIZE_FILE.tmp"
            echo "d${MONTH}_SizeKB=\"${DEVELOP_SIZE}\"" >> "$MONTHLY_SIZE_FILE.tmp"
            echo "d${MONTH}_CheckTime=\"${CHECK_TIME}\"" >> "$MONTHLY_SIZE_FILE.tmp"
          fi
          # 写入 construct 的键值（仅当有变化时）
          if [ "$c_changed" = "true" ]; then
            echo "c${MONTH}_ETAG=\"${{ steps.get-etag.outputs.CONSTRU_ETAG }}\"" >> "$MONTHLY_SIZE_FILE.tmp"
            echo "c${MONTH}_MD5=\"${MD5_CONSTRU_GZ}\"" >> "$MONTHLY_SIZE_FILE.tmp"
            echo "c${MONTH}_SizeKB=\"${CONSTRU_SIZE}\"" >> "$MONTHLY_SIZE_FILE.tmp"
            echo "c${MONTH}_CheckTime=\"${CHECK_TIME}\"" >> "$MONTHLY_SIZE_FILE.tmp"
          fi
          # 替换原文件
          mv "$MONTHLY_SIZE_FILE.tmp" "$MONTHLY_SIZE_FILE"
          
          # 打印写入结果
          echo "✅ monthly_size.txt 写入完成，内容如下："
          cat "$MONTHLY_SIZE_FILE"

          # ====================== 原有逻辑：输出最终结论 ======================
          echo -e "\n======= 【核心】最终判定 ======="
          echo "📌 最终结论：Data updated! Saving new .gz (ETag/MD5均变化)"

      # 修复5：第二次取消工作流（MD5 检查，c/d 均无变化时执行）
      - name: 取消工作流（MD5 无变化，c/d 均无更新）
        if: steps.check.outputs.changed == 'false'
        uses: conbanwa/cancel-workflow/.github/actions/cancel-workflow@v1.0.0
        with:
          cancel-reason: 'MD5 无变化（c/d 均一致），无需提交，自动取消工作流'

      - name: Commit and push .gz if changed
        if: steps.check.outputs.changed == 'true'
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git lfs track "drop*.gz"
          git add dump/
          # 提交信息包含 c/d 变更状态
          COMMIT_MSG="Update "
          if [ "${{ steps.pre-check.outputs.d_changed }}" = "true" ]; then
            COMMIT_MSG+="construct "
          fi
          if [ "${{ steps.pre-check.outputs.c_changed }}" = "true" ]; then
            COMMIT_MSG+="develop "
          fi
          COMMIT_MSG+="${{ steps.set-month.outputs.month }}.gz"
          git commit -m "$COMMIT_MSG"
          git push --force-with-lease
